{
  "configurations": [
    {
      "name": "Attach to Edge",
      "port": 5500,
      "request": "attach",
      "type": "msedge",
      "webRoot": "${workspaceFolder}"
    },

    {
      "name": "Launch Chrome",
      "request": "launch",
      "type": "chrome",
      "url": "http://localhost:5500",
      "webRoot": "${workspaceFolder}"
    },
  {
    "name": "Attach to Chrome",
    "port": 5500,
    "request": "attach",
    "type": "chrome",
    "webRoot": "${workspaceFolder}"
  },
  {
    "name": "Launch Chrome 2",
    "request": "launch",
    "type": "chrome",
    "url": "http://localhost:5500",
    "webRoot": "${workspaceFolder}"
  }
  ]

}

